import React from 'react';
import SavedCard from '../SavedCard';

const Saved = (props) => {
    return (
        <div>
            <SavedCard {...props}/>
        </div>
    )
};

export default Saved;